Register in mongodb atlas
create new project
create new cluster and user
connect cluster and select driver
change config.env DATABASE and DATABASE_PASSWORD
note(change <password> to <PASSWORD> in DATABASE)